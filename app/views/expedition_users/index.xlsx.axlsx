xlsx_package.workbook.add_worksheet(name: "#{controller_name.titleize}") do |sheet|
  sheet.add_row ["ID", "Name", "Expedition", "Role"]
  @items.each do |user|
    sheet.add_row [user.id, user.user.name, user.expedition.name, user.expedition_role_type]
  end
end
